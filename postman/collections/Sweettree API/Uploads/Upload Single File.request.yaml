$kind: http-request
name: Upload Single File
method: POST
url: '{{baseUrl}}/api/uploads'
order: 1000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{token}}'
body:
  type: formdata
  content:
    - key: file
      type: file
      src: ''
