$kind: http-request
name: Duplicate Combo
method: POST
url: '{{baseUrl}}/api/combos/admin/:id/duplicate'
order: 5000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{token}}'
pathVariables:
  - key: id
    value: COMBO_ID
