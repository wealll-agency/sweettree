$kind: http-request
name: Delete Combo
method: DELETE
url: '{{baseUrl}}/api/combos/admin/:id'
order: 4000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{token}}'
pathVariables:
  - key: id
    value: COMBO_ID
