$kind: http-request
name: Get Stock Notifications Admin
method: GET
url: '{{baseUrl}}/api/notifications/admin/stock'
order: 3000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{token}}'
