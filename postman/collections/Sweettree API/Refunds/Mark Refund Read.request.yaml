$kind: http-request
name: Mark Refund Read
method: PATCH
url: '{{baseUrl}}/api/refunds/:id/read'
order: 3000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{token}}'
pathVariables:
  - key: id
    value: REFUND_ID
