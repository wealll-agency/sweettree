$kind: http-request
name: Get Warehouse By ID
method: GET
url: '{{baseUrl}}/api/warehouses/:id'
order: 3000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{token}}'
pathVariables:
  - key: id
    value: WAREHOUSE_ID
