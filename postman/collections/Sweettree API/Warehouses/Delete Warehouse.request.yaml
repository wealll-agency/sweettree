$kind: http-request
name: Delete Warehouse
method: DELETE
url: '{{baseUrl}}/api/warehouses/:id'
order: 5000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{token}}'
pathVariables:
  - key: id
    value: WAREHOUSE_ID
