$kind: http-request
name: Mark Inventory Read
method: PATCH
url: '{{baseUrl}}/api/products/inventory/:id/read'
order: 8000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{token}}'
pathVariables:
  - key: id
    value: PRODUCT_ID
